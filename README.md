# Cart API

## Setup

Run once to initialize the project:

```bash
./setup_project.sh
```


## Start

```bash
docker compose up -d
```

The API is available at: http://localhost:8080

## Stop

```bash
docker compose down
```
